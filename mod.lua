function data()
return {
	info = {
		minorVersion = 0,
		severityAdd = "NONE",
		severityRemove = "NONE",
		name = _("More Tracks for Stations"),
		description = _("desc"),
	},
	runFn = function (settings)
		local stationmod = require "stationmod"
	end
}
end
